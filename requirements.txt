# ============================================================
# BRN Node — dependências de runtime
# Python >= 3.10 (usa PEP 585: dict[str, int], tuple[str, int])
# ============================================================

# --------- Criptografia / carteira ---------
cryptography>=42.0.0
argon2-cffi>=23.1.0
ecdsa>=0.19.0

# --------- Rede P2P ---------
orjson>=3.10.0            # serialização rápida (usada em p2p.py)
pyngrok>=7.2.0            # túnel TCP público (p2p.py)
python-dotenv>=1.0.0      # .env loading

# --------- Web / API / GUI ---------
flask>=3.0.0
flask-limiter>=3.5.0
pywebview>=5.0            # GUI desktop (opcional)
# tkinter já vem com Python padrão

# --------- Banco (built-in, mas extras úteis) ---------
# sqlite3 já é built-in
# aiosqlite>=0.20.0       # se quiser persistência 100% async

# --------- BSC / PancakeSwap (bridge futura — opcional) ---------
# web3>=6.20.0
# eth-account>=0.13.0
# requests>=2.32.0
# aiohttp>=3.10.0
# websockets>=13.0
# tenacity>=8.5.0

# --------- UX de terminal (opcional) ---------
# rich>=13.9.0
# tabulate>=0.9.0